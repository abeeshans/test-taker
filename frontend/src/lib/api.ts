// Hardcoding API URL to ensure consistency and avoid env var issues
export const API_URL = 'http://localhost:5000'; // Using localhost to match frontend origin for CORS
console.log('API_URL set to:', API_URL);
